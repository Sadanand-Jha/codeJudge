import PreparationWorkspace from "@/components/preparation/PreparationWorkspace";

export default function PreparationLayout({ children }: { children: React.ReactNode }) {
  return <PreparationWorkspace>{children}</PreparationWorkspace>;
}
